# loteria-cards

This has moved. The play-along board and the tabla maker now live in [markjf99702/el-cantor](https://github.com/markjf99702/el-cantor), next to El Cantor the caller, in one app with a home screen.

`index.html` and `print.html` here only forward to the new pages, so old bookmarks and **Share** links still work. A shared-tablas link keeps its `#t=…` part on the way. Tablas saved on a device carry over, because both sites are served from the same `markjf99702.github.io` address.
